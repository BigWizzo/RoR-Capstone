<% content_for :h1, "SUBJECT CLOCKINGS" %>
<% content_for :study_mate, "SUBJECT CLOCKINGS" %>
<% content_for :h4, "Subject Based Clockings" %>
<%= render 'subjects/top' %>
<div class="container-fluid">
  <div class="row content">
  <%= render 'subjects/nav' %>
    <div class="col-md-9 content-wrap">
      <%= render 'subjects/clock' %>
    </div>
  </div>
</div>
<%= render "subjects/footer"%>